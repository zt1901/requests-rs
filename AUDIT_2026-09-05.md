# requests-rs 0.4.0 构建源码审计与修复记录

审计日期：2026-09-05（本地时区 Asia/Shanghai）。基线：`8029c8cc1c14a6cf9e2c22fda7000d745a5a5381`，审计开始时普通 Git 核对 main 和 v0.4.0 均指向该提交。以下修复是未提交的工作区改动，不代表已发布版本；包版本仍为 0.4.0。

## 覆盖范围

- 完整阅读自有构建/运行入口：`src/lib.rs`、`src/profile_http3.rs`、`src/api_wrapper.py`、`src/main.rs`、`src/bin/benchmark_connect_proxy.rs`、`python/requests_rs/`、`build_and_install.py`、`scripts/同步指纹.py`、Cargo/Python 元数据和 wheel CI。CLI/benchmark 二进制不等同于公开 Python API。
- 依据 Cargo 实际依赖图清点六个本地 path crate：`vendor/btls-wrapper`、`vendor/btls/btls-sys`、`vendor/http2`、`vendor/quiche/quiche`、`vendor/quiche/octets`、`vendor/wreq`。对源码做风险模式扫描，重点审查本地定制差异、TLS 构建/验证、HTTP2 输入断言、HTTP3/QPACK、QUIC 参数、FFI 初始化。
- 对 267 个锁定 Rust 包进行 RustSec 公告扫描。第三方数十 MB 源码并非全部逐行人工审计；扫描不等于证明没有漏洞，未启用的 FIPS/qlog 等路径未冒充运行时覆盖。

## 已修复的问题

| 类别 | 原有负面影响 | 修复与回归 |
|---|---|---|
| 原生输入安全 | 循环/过深 JSON 可耗尽原生栈；非法 H2 指纹可触发断言或生成非法帧 | 限定递归、保留 Python 循环检测；校验 SETTINGS/window/priority/pseudoheaders |
| 连接池/超时 | 八个同步异步入口在连接池耗尽时可能无限等候 | 连接槽获取有界并扣除排队预算，H3 回退同样受总预算约束；持有唯一流槽回归覆盖八入口 |
| 流与取消 | 流式响应绕过 max_response_bytes；读取竞争截断内容；取消可遗留资源 | 累计限制解码字节并释放槽；单消费者保护；异步关闭可在取消后继续等待；修复未轮询 future 的路由预留清理 |
| Cookie | 分号/控制字符可注入 Cookie 或扩大 cookie 属性作用域 | Python 与 native 边界分别校验，拒绝属性/分隔符注入 |
| CONNECT 代理 | 头读取在检查前无限分配；隧道预读丢字节；半关闭/凭据解码和计量错误 | 有界读取、保留缓冲、传播半关闭、正确解码 URL 用户信息及统计 CRLF |
| H3 身份和请求隔离 | 无 SNI 时漏掉 DNS/IP 身份校验；捕获模板可能回放旧 Cookie/Auth/上下文 | 身份验证独立于 SNI；统一可信根和 IPv6 处理；仅允许稳定头默认值，正确映射 Host |
| H3 响应和参数 | 1xx/trailer 混入最终头；无 body 响应错误解压；长度/参数溢出 | 分离头阶段，校验 Content-Length、HEAD/204/304、TP/SETTINGS 数值和溢出 |
| H3 调度和 DNS | 阻塞队列/DNS 未计入超时；绕过自定义 DNS | 可取消的异步排队和 DNS 总预算；运行中 QUIC 在 UDP/HTTP3/分块解压边界协作取消并清理缓存连接；有自定义 resolver 时不走绕过它的直接 H3；修复 HEAD 303 |
| QUIC/QPACK/FFI | QPACK 整数截断溢出；保留参数 ID 下溢；部分 C 初始化后读取未初始化 Rust 存储 | 严格整数边界、先验证 ID、AEAD/AES 存储预先置零；动态 QPACK 接收支持见后续专项行，非法编码仍明确拒绝 |
| 动态 QPACK 后续专项 | 宣告动态表能力但未处理 encoder stream，合法动态响应失败 | 实现容量/驱逐、增量指令、Huffman、RIC/Base/引用、阻塞与恢复、ACK/插入/取消反馈及部分写入；30 项独立真实依赖回归及 3 项确定种子性质测试 |
| 构建一致性 | 本地构建默认覆盖已提交指纹；同步失败可能截断文件；TLS 源码改变未触发重建 | 指纹同步显式 opt-in 且原子替换；工具预检/尊重环境；--locked；补齐 BoringSSL/CMake/外部输入失效依赖；所有原生构建显式启用 PIC，Linux 调试链接重新验证通过 |
| 已公告依赖问题 | PyO3 0.28.3 两条安全公告、lru 0.18.1 一条 unsound 警告 | 定点升级 PyO3 家族 0.29.2、async runtime 0.29.0、lru 0.18.4，仅七个锁定包变化 |
| API 参数陷阱 | bool timeout 被当秒数，字符串被拆成 WS 子协议字符，重定向数无效 | 明确拒绝错误类型/范围；补全异步响应上下文管理 |

唯一公开导入 `from requests_rs import requests`、模块默认 chrome152/HTTP2、捕获指纹全协议自动应用保持不变；未新增 requests_rust 兼容入口或 fingerprint_scope 开关。

## 实际验证结果

- `cargo test --locked --offline --all-targets`：Windows x64 和 Linux x86_64 各 55 项通过：21 个库测试、30 个 QPACK 回归（含完整连接级 16 MiB 阻塞缓冲和 64 KiB 反馈队列边界/释放）、3 个确定种子性质测试和 1 个 HTTP2 extended CONNECT WebSocket 集成测试；两个 binary 编译通过。
- `python -m unittest test_audit_python_api test_audit_build test_audit_rust_api test_audit_http3 -v`：32 项通过，使用新构建 native。
- 将 Python API 审计套件绑定到新 wheel 内嵌 wrapper：另 13 项通过，不仅检查仓库源 wrapper。
- 既有 `test_requests_rs_import.py`、`test_python_api.py`、`test_proxy_preauth.py`、`test_socks5_auth.py`、`test_http3_schema2_validation.py`、`test_websocket_api.py`、`test_http3.py` 七个脚本通过。涉及本地 HTTP/HTTPS/WSS、代理认证、QUIC 复用/压缩/schema2/回退和异步路径。
- 以上 Python/wheel/七个既有脚本在 Windows 和 Linux 均通过；两平台另通过运行中 QUIC 取消回归。独立 aioquic 1.3.0 / pylsqpack 0.3.24 TLS 互操作各通过：同连接 8 次响应、7 个动态 QPACK section、2 批延迟 encoder 指令及 decoder 反馈。
- 确定种子测试包含独立动态表模型、分片差分、4096 组恶意指令、24576 组 Huffman/header 变异及 99 个整数探针；不是长期覆盖引导 fuzz。
- CI 已纳入四组新增 Python 审计套件、运行中取消、固定独立对端版本的互操作和原生 `--all-targets`（含 QPACK/性质测试）。`git diff --check` 通过（仅换行提示）。
- RustSec 数据库普通 Git 提交 `5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5`（2026-09-02）；cargo-audit 0.22.2 以 `--no-fetch --no-yanked` 检查修复后锁文件：267 包，0 个漏洞、0 个警告。未使用 GitHub REST/GraphQL 或账号 API 配额。

## 构建产物与证据

Windows x64 与 Linux x86_64 wheel 均以 release/locked 构建并隔离安装验证，未覆盖原 dist 或全局包。最终证据目录：`D:\实验项目\.audit-requests-rs-20260905\hardening`。

| 平台 | 相对证据目录路径 | bytes | SHA256 |
|---|---|---:|---|
| Windows x64 | `wheels/requests_rs-0.4.0-cp310-abi3-win_amd64.whl` | 5044526 | `dc68164703fcce12c8972a4cda4dc00a799d3b14b000e1594aa90e4f56a3b9ea` |
| Linux x86_64 | `linux/wheels/requests_rs-0.4.0-cp310-abi3-manylinux_2_38_x86_64.whl` | 4780091 | `fe8a00cd6d78aad0c942a1342563d3137192bdc44e02446b514bbafdb835541a` |

- `final-verification.json`：双平台最终摘要；`final-source-match.json`：17 个关键源码/测试与 Linux 构建副本一致。
- `windows-all-targets-final.log`、`linux/cargo-all-targets-final.log`：各 55 项原生测试通过。
- Windows `regression-results.json` 11 个任务退出 0；Linux `regression-results.json` 9 个任务退出 0，另 `cancel-linux.log`、`interop-linux.log` 通过。
- `linux/link-diagnosis.md`：Linux 调试原生对象 PIC 诊断。修正后完整 all-targets 与 release wheel 均实际通过。
- 首轮审计及 QPACK 后续证据保留于父目录和 `qpack-followup`，不应将其旧 wheel 当作最终产物。

## 未解决或未验证的边界

1. **动态 QPACK 接收方向已实现并通过专项及独立对端回归**：不降低捕获 SETTINGS；发送方向仍采用合法静态编码。16 MiB 阻塞缓冲、64 KiB 反馈队列的连接级边界、拒绝及恢复已验证。尚未完成公网多实现矩阵和长期覆盖引导 fuzz，不能声称全面浏览器 H3 兼容。
2. **运行中 QUIC 已支持协作取消**，包含排队与运行中回归；不是强制终止线程。单次 TLS/codec 调用和已进入 OS 的同步 DNS resolver 无法中途强停，取消延迟受这些调用约束。
3. 本轮实际构建并验证 Windows x64 和 Linux x86_64（manylinux_2_38）。macOS、ARM、旧 glibc 及完整 CI 矩阵未实际验证，不能以 x64 成功代替。
4. vendored quiche 的完整上游单测未运行；根 crate 直接使用真实 quiche 依赖的 30 项 QPACK/连接及 3 项性质测试已在双平台通过，实际源码轻量 harness 另通过 25 项模块测试，但不冒充完整上游套件。
5. RustSec 扫描未包含 yank 检查，不替代 C/BoringSSL 的完整漏洞审计。独立 aioquic/pylsqpack 是本地 TLS 对端，不等同于公网多服务端认证或零缺陷证明。

原有 benchmark/Facebook 改动、产物和 `vendor/btls/btls/src/ssl/mod.rs` 原有 dirty 状态保留。未提交、未打标签、未发布、未读取发布凭据。


## 2026-09-06：完整上游测试补跑（尚未全部通过）

在隔离 Linux 副本中，以 custom-client-dcid 特性执行 quiche 完整库测试，并将 btls-sys 指向实际 vendored 原生实现；独立 workspace 解析测试依赖，没有修改主项目 Cargo.lock。

- 修复上游 tests.rs 两处 TransportParams 构造遗漏 raw_wire_override 导致的编译失败。
- 完整测试清单有 1061 项；首轮出现失败及迁移/路径探测长运行，已停止该次执行。单独迁移测试 10 秒超时可复现，不能宣称完整套件通过。
- HTTP/3 子集独立执行 177 项。校正七个测试的过时假设：当前 SETTINGS 编码仅发送正式 DATAGRAM ID、按定制顺序编码；测试 encoder 指令须协商非零 QPACK 容量。仅改测试，不降低生产协议验证或配额。复跑结果 **173 通过、4 失败**（此前166通过、11失败）。
- 剩余 H3 失败：两处 stream 测试的 SETTINGS 顺序断言；0-RTT 和 RESET_STREAM 两处固定报文长度断言。尚需逐项确认并回归，未忽略或标为通过。
- 其他传输/PMTUD/迁移失败尚需继续有界诊断。本轮未改生产运行逻辑、重发 wheel 或提交发布。此前双平台55项测试结论仅适用于该项目回归范围，不覆盖此次新增的完整上游套件。

证据：`D:\实验项目\.audit-requests-rs-20260905\upstream-20260906` 下 `quiche-full.log`、`bounded-full.log`、`h3.log`、`h3-repaired.log` 和运行脚本。

### 2026-09-06 HTTP3 上游测试后续

- 独立 quiche workspace 的 HTTP3 子集最新实际结果：176 通过、1 失败（共177项），证据 `upstream-20260906/h3-repaired.log`。
- 修复测试对本 fork SETTINGS 编码顺序的过时预期；0-RTT 数据包长度改引用实际 `MIN_CLIENT_INITIAL_LEN`；reset 测试不再将 ACK 长度写死，仍验证收到 Reset 及重复抑制。没有放松生产协议检查。
- 剩余 `h3_handshake_0rtt` 在服务端 undecryptable 队列期望0、实际1处失败，不能仅改期望掩盖，需继续核查早期密钥/握手推进。完整传输套件的迁移、路径验证长运行及其他失败仍未关闭。当前不得声称完整上游测试通过。

2026-09-06：H3 剩余 0-RTT 失败定位为测试仅交付首个 Initial，定制 TLS ClientHello 分片后服务端尚无 early-data 密钥。测试改为交付完整 Initial flight，并新增仍处于 early-data、未完成握手断言；不修改生产 TLS/QUIC。H3 子集实际 177/177 通过，日志 upstream-20260906/h3-repaired.log。


### 2026-09-06 MTU/路径验证修复

将协议最低 UDP payload 1200 与指纹 Initial padding 1250 分离；PMTUD 下限和 PATH_RESPONSE 的 MTU 验证使用协议常量，Initial 指纹保持不变。path 单测使用协议门槛验证低于/达到下限。逐项有界重新执行全部 1061 项：999 通过、62 失败、0 超时；此前 16 个超时用例全部通过。失败尚需独立排查，不代表完整上游测试通过。本次生产修改尚未重建 wheel，之前产物不包含本修复。证据 upstream-20260906/classified-mtu.json、mtu-build.log。

### 2026-09-06 CID / 0-RTT 回归续查
修正完整 ClientHello flight 的测试驱动，保留非法 CID 拒绝、早期数据解密与非 established 状态断言；ALPN 关闭包长度使用定制 Initial 常量。隔离 quiche 完整 1061 项逐项有界执行：1017 通过、44 失败、0 超时。证据 upstream-20260906/classified-early.json 与 early-build.log。当前修改未重新构建 wheel，仍有拥塞控制容量、ACK 包号及握手计量相关失败，未声称完整通过。

### 2026-09-06 RESET_STREAM ACK 回归
两项 RESET_STREAM 测试原先硬编码 ACK 到包号 5/11，会确认当前定制握手未发送的包。改为根据实际已发送的 Application 最大包号构造 ACK，保留 StreamStopped、流回收和重复 RESET 抑制断言，未放宽生产 ACK 校验。完整 1061 项逐项有界复跑：1025 通过、36 失败、0 超时。证据 upstream-20260906/classified-ack.json 与 ack-build.log。最新生产 MTU 修复仍未重建 wheel。

### 2026-09-06 ACK 范围验证后续
- 两组单路径/多路径 ACK 验证改用实际发送包号，保留已确认包、待确认数量、跨路径最大包号及对未发送包 ACK 的 ProtocolViolation 断言。未修改生产 ACK 校验。
- 隔离完整库编译及 1061 项逐项有界测试：1029 通过、32 失败、0 超时。证据 upstream-20260906/classified-ack-range.json 与 ack-range-build.log。最新生产 MTU 修改尚未重建 wheel。

### 2026-09-06 握手回调测试跟进
两组握手回调测试改为交付完整 Initial/server flight，而非假定单个数据报已包含全部 TLS 消息；保留 CWND、idle timeout、MAX_STREAMS 及阈值断言，不修改生产回调逻辑。完整 1061 项逐项有界执行：1033 通过、28 失败、0 超时。证据 upstream-20260906/classified-callback.json、callback-build.log。尚未重建包含最新 MTU 修改的 wheel，仍未提交或发布。

### 2026-09-06：Initial/路径探测测试后续
仅校正测试：Initial padding 使用指纹常量，PATH_CHALLENGE 使用协议最低 MTU；补齐分片 ClientHello，合包 padding 受已协商发送上限约束。保留 PTO 探测数量、握手密钥、损坏包处理、实际收发计数等断言。完整 1061 项逐项有界复跑：1045 通过、16 失败、0 超时。证据 upstream-20260906/classified-packet.json 及 packet-build.log。剩余 BBR2 容量和反放大握手计量仍待处理；最新生产 MTU 改动尚未重建 wheel。

### 2026-09-06：反放大外层填充计量修复

扩大仅测试使用的证书链，确保握手真实耗尽反放大预算，保留精确倍率断言。进一步确认并修复生产缺陷：send_single 已计量包内字节，但 send_on_path 的数据报外层填充未扣除预算，导致剩余虚假额度及耗尽统计缺失。现在额外填充同样扣账，回归明确检查剩余额度为零。完整 1061 项逐项有界测试：1048 通过、13 失败、0 超时；反放大测试全部通过。剩余为 BBR2 容量相关测试，尚未宣称全套通过。证据 upstream-20260906/classified-padding.json、padding-build.log。最新生产修改尚未重建 wheel，旧产物不代表当前源码。

### 2026-09-06：满拥塞窗口 ACK 回归

两组 ACK-only 测试改为按当前 recovery 可用窗口填满发送缓冲，不再依赖 TLS 握手字节数决定的 BBR2 固定值；仍校验窗口耗尽和输出仅包含 ACK。Linux 全部 1061 项逐项有界执行：1050 通过、11 失败、0 超时。证据 `upstream-20260906/classified-bbr-ack.json` 和 `bbr-ack-build.log`。本次仅测试修改，未提交发布；最新生产修复仍待重建 wheel。

### 2026-09-06：发送容量预算回归

将 send_capacity 测试中依赖旧 TLS 握手字节数的 BBR2 常数替换为写入前 recovery 窗口快照；逐次写入仍精确验证预算扣减、耗尽及 Done，Cubic 固定初始窗口断言保留。完整 1061 项逐项有界执行：1052 通过、9 失败、0 超时。证据 upstream-20260906/classified-capacity.json；本次未修改生产运行逻辑，最新生产修改仍待 wheel 重建。

### 2026-09-06 BBR2 流重置回归

两组流重置/丢包测试不再假设 TLS 握手后的 BBR2 窗口恒为 13878；写入前取实际可用窗口并验证范围、tx_cap 和 Cubic 基线。保留重置后缓冲归零及精确窗口增长关系：无损为初始容量两倍，丢一个 1200 字节包时为两倍减 1200。未更改生产拥塞控制。

Linux 完整库重新编译，1061 项逐项有界执行：1058 通过、3 失败、0 超时。证据 upstream-20260906/classified-reset.json、classified-reset.log、reset-build.log。剩余 initial_cwnd、resilience_against_migration_attack、update_max_datagram_size 各一个 BBR2 参数化测试；最新 MTU/反放大生产修改仍未重建 wheel。

## 上游套件最终逐项验证（U19）

最后三项 BBR2 测试已移除依赖固定 TLS 字节数的容量预期：初始窗口在握手前精确验证配置，握手后验证窗口与累计发送字节上界；MSS 仍精确校验 1500→1200；迁移测试以实际可用拥塞窗口确定首批发送容量并保留后续攻击防护断言。未修改 BBR2 生产算法。

Linux 隔离 quiche 库完成编译，并对本配置列出的全部 **1061 项测试**逐项执行（每项15秒上限、并发2）：**1061通过、0失败、0超时**。证据：`upstream-20260906/classified-final-bbr.json`、`classified-final-bbr.log`、`final-bbr-build.log`。这是 `custom-client-dcid` 配置的库测试覆盖，不代表全部feature组合。主项目依赖锁文件未因独立workspace解析而改动。

此前 MTU/路径验证和反放大计量的生产修改尚待重新构建 Windows/Linux wheel 并执行包级回归；报告早前 wheel 哈希不能代表这些最新修改。未提交或发布。


## 最新双平台构建闭环（证据目录 final-rebuild-20260907）

最新 MTU/反放大生产修复已编入两平台 release/locked/offline wheel，并分别隔离安装验证。Windows/Linux `cargo test --locked --offline --all-targets` 各55项通过；两平台32项Python审计、13项内嵌wrapper、7个既有协议/API脚本、运行中取消及独立aioquic动态QPACK互操作通过。此前Linux独立quiche库1061项全通过。

- Windows x64: `D:\实验项目\.audit-requests-rs-20260905\final-rebuild-20260907\wheels\requests_rs-0.4.0-cp310-abi3-win_amd64.whl`；5044846 bytes；SHA256 `24edd099ce3336d1c11c4570079a796e6e3131abd7ce16494bee061a558d11f8`。
- Linux x86_64: `D:\实验项目\.audit-requests-rs-20260905\final-rebuild-20260907\linux\wheels\requests_rs-0.4.0-cp310-abi3-manylinux_2_38_x86_64.whl`；4780649 bytes；SHA256 `3625e8e9cffff1f350360d693a6be4da8c3d24f1f8dc294dc30712014eb6fa2f`。

以上产物取代本文前面历史构建哈希作为当前源码验证产物；历史日志仍保留。证据verification.json记录关键源码与Linux副本一致，原生日志及回归任务全部退出0。未覆盖macOS/ARM/旧glibc或长期fuzz；未提交、发布或覆盖原dist。
